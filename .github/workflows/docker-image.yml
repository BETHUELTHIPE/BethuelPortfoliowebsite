name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      db:
        image: postgres:latest
        env:
          POSTGRES_DB: Bethuelresumedb
          POSTGRES_USER: Bethuel
          POSTGRES_PASSWORD: 23498812
        ports:
          - 5432:5432
        volumes:
          - pg_data:/var/lib/postgresql/data

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.bethuelm/portfolio-app }}  # Add your Docker Hub username to Secrets
        password: ${{ secrets.DOCKER_PASSWORD }}  # Add your Docker Hub password to Secrets

    - name: Build Docker image
      run: docker build . -t bethuelm/portfolio-app:latest

    - name: Run tests
      run: |
        docker-compose up -d
        docker-compose exec web python manage.py test  # Adjust if necessary
        docker-compose down

    - name: Push Docker image to Docker Hub
      run: docker pushbethuelm/portfolio-app:latest

volumes:
  pg_data:

